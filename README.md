# Revision Assistant

A lightweight backend starter for the Revision Assistant project.

## Structure

- Backend app code lives in `backend/app`
- Tests live in `backend/tests`

## Running tests

```bash
cd backend
pytest
```
